const prompt = require("prompt-sync")();
let media;

for (i = 0; i <= 10; i++) {
  let n = Number(prompt("Digite um número maior que 0: "));
}
if ((n = i)) {
  for (n = 0; n <= 10; media / 10) {
    media = n + n + n + n + n + n + n + n + n + n;
  }

  console.log("A média é", media);
}
